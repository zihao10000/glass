<Window x:Class="GlassWarehouseSystem.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="系统运行日志检索"
        Height="750" Width="1100"
        Background="#F0F2F5"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        FontFamily="Microsoft YaHei">

    <Window.Resources>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DCDFE6"/>
            <Setter Property="Foreground" Value="#303133"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#409EFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#E0E0E0" Opacity="0.4" ShadowDepth="1"/>
            </Border.Effect>
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="开始时间:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker x:Name="dpStart" Width="130" Height="30" VerticalContentAlignment="Center"/>
                    <TextBlock Text=" - " VerticalAlignment="Center" Margin="5,0"/>
                    <DatePicker x:Name="dpEnd" Width="130" Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="日志级别:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="cmbLogLevel" Width="100" Height="30" SelectedIndex="0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="Info"/>
                        <ComboBoxItem Content="Warn"/>
                        <ComboBoxItem Content="Error"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="关键字:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="txtKeyword" Width="180" Height="30" VerticalContentAlignment="Center" Padding="5,0"/>
                </StackPanel>

                <Button Content="🔍 开始查询" Style="{StaticResource PrimaryBtn}" Click="BtnSearch_Click"/>
                <Button Content="🔄 刷新全部" Style="{StaticResource PrimaryBtn}" Background="#67C23A" Click="BtnReset_Click"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="4" BorderBrush="#EBEEF5" BorderThickness="1">
            <DataGrid x:Name="LogDataGrid" 
          ItemsSource="{Binding LogList}"
          SelectionMode="Extended"      SelectionUnit="FullRow"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F2F6FC"
                      BorderThickness="0"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F5F7FA"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,0"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="编号" Binding="{Binding LogID}" Width="70"/>
                    
                    <DataGridTemplateColumn Header="级别" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding TypeColor}" CornerRadius="3" Padding="5,2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Type}" Foreground="White" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="记录时间" Binding="{Binding RecordTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170"/>

                    <DataGridTextColumn Header="内容" Binding="{Binding LogContent}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="总条数:" FontWeight="Bold"/>
                <TextBlock Text="{Binding LogList.Count, FallbackValue=0}" Foreground="#409EFF" FontWeight="Bold" Margin="8,0" FontSize="16"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="清除" Style="{StaticResource ActionBtn}" Click="BtnClearDisplay_Click"/>
                <Button Content="清除全部" Style="{StaticResource ActionBtn}" Click="BtnClearAll_Click" Foreground="#CF2222"/>
                <Button Content="退出" Style="{StaticResource ActionBtn}" Click="BtnExit_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>